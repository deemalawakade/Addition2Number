<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.deepmala.com.designing_xml">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LinearL_RelativeL"
            android:label="@string/title_activity_linear_l__relative_l"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PassingDataUsingIntent" />
        <activity android:name=".ImageLoader" />
        <activity android:name=".activity.WorkingInActivity"></activity>
    </application>

</manifest>